{
  "private": "true",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "changeset:init": "npx changeset init",
    "changeset:add": "changeset add",
    "clean-install": "pnpm -r exec rm -rf node_modules && pnpm i",
    "--  dev  --------------------------------------------------": "",
    "pre:dev": "wait-on ./packages/tiny-player/dist/index.min.js",
    "dev": "concurrently \"pnpm dev:player\" \"pnpm run pre:dev && pnpm dev:doc\"",
    "dev2": "concurrently \"pnpm dev:player\" \"pnpm run pre:dev && pnpm dev:playground\"",
    "dev:parallel": "pnpm -r --no-sort dev",
    "dev:player": "pnpm -F \"tiny-player\" dev",
    "dev:doc": "pnpm -F doc dev",
    "dev:playground": "pnpm -F playground dev",
    "--  build  ------------------------------------------------": "",
    "build": "pnpm -r --sort build",
    "build:player": "pnpm -F \"tiny-player\" build",
    "build:playground": "pnpm -F playground build",
    "--  publish  ----------------------------------------------": "",
    "publish": "pnpm -F \"tiny-player\" publish"
  },
  "author": "荣顶",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "concurrently": "^8.0.1",
    "wait-on": "^7.0.1"
  }
}
